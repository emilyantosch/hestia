Use `just` for Rust commands. The frontend uses GPUI Kit, not Qt. On macOS, build with the native Rust toolchain and Xcode. On Linux, use `nix develop --command just <recipe>` for GPUI's system libraries.
